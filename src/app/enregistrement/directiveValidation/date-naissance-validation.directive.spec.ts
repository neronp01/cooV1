import { DateNaissanceValidationDirective } from './date-naissance-validation.directive';

describe('DateNaissanceValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new DateNaissanceValidationDirective();
    expect(directive).toBeTruthy();
  });
});
