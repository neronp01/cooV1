import { HideIconeDirective } from './hide-icone.directive';

describe('HideIconeDirective', () => {
  it('should create an instance', () => {
    const directive = new HideIconeDirective();
    expect(directive).toBeTruthy();
  });
});
